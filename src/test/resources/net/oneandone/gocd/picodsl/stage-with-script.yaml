format_version: 3
pipelines:
  p1:
    group: dev
    label_template: ${material1}
    lock_behavior: unlockWhenFinished
    materials:
      material1:
        package: material1
    stages:
      - DEV:
          approval: manual
          jobs:
            jobOne:
              tasks:
                - script: echo one
            jobTwo:
              tasks:
                - script: echo two
  p2:
    group: qa
    label_template: ${p1}
    lock_behavior: unlockWhenFinished
    materials:
      p1:
        pipeline: p1
        stage: DEV
    template: t1